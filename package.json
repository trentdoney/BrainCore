{
  "name": "braincore",
  "version": "1.1.5",
  "description": "Autonomous memory system for AI infrastructure — extracts, preserves, and retrieves operational knowledge from incidents, sessions, and monitoring data",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/trentdoney/BrainCore.git"
  },
  "license": "MIT",
  "scripts": {
    "braincore": "bun run src/cli.ts",
    "lint": "tsc --noEmit",
    "test": "bun test",
    "sanitize:check": "bash scripts/pre-push-gate.sh"
  },
  "dependencies": {
    "postgres": "^3.4.5",
    "zod": "^4.3.6",
    "yaml": "^2.8.2"
  },
  "devDependencies": {
    "bun-types": "1.3.8",
    "typescript": "^5.9.3"
  }
}
